# MkDocs Material Migration — ziviSpaceEchoJS

This package targets:

```text
https://github.com/vicvalentim/ziviSpaceEchoJS
```

The **repository name** is:

```text
ziviSpaceEchoJS
```

The **plugin display name remains**:

```text
ziviSpaceEcho
```

## What changed

- `site_url` updated to `https://vicvalentim.github.io/ziviSpaceEchoJS/`.
- `repo_url` updated to `https://github.com/vicvalentim/ziviSpaceEchoJS`.
- `repo_name` updated to `vicvalentim/ziviSpaceEchoJS`.
- Raw download links updated to `ziviSpaceEchoJS`.
- MkDocs Material workflow preserved.
- JSFX internal title preserved as `ziviSpaceEcho`.

## Apply locally

From the repository root:

```bash
unzip ziviSpaceEchoJS_mkdocs_material_site_v7_6_2.zip -d /tmp/zivi_mkdocs
cp -R /tmp/zivi_mkdocs/ziviSpaceEchoJS_mkdocs_material_site_v7_6_2/* .
git add .
git commit -m "Update documentation for ziviSpaceEchoJS repository"
git push
```

## GitHub Pages settings

After the workflow runs successfully, set Pages to:

```text
Settings → Pages
Source: Deploy from a branch
Branch: gh-pages
Folder: / root
```

Public documentation URL:

```text
https://vicvalentim.github.io/ziviSpaceEchoJS/
```
